# DTO-DSS Backend Requirements
# Phase 3: RandomForest + SHAP

# FastAPI
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
pydantic>=2.0.0

# ML
scikit-learn>=1.3.0
numpy>=1.24.0
joblib>=1.3.0
pandas>=2.0.0

# SHAP (XAI)
shap>=0.42.0

# Visualization (Streamlit Dashboard)
streamlit>=1.30.0
graphviz>=0.20.0

# Utilities
python-multipart>=0.0.6

# RDKit (chemical informatics) - recommended to install via conda:
#   conda install -c conda-forge rdkit
# If you use pip-only envs, ensure an appropriate rdkit build is available.

# Testing
pytest>=7.4.0
pytest-asyncio>=0.21.0
httpx>=0.25.0
